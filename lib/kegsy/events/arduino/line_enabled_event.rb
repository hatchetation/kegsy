module Kegsy
  module Events
    module Arduino
      
      class LineEnabledEvent

        def initialize(options={})
          # TODO Persist event so that we can backfill our db
          # TODO Enable the line identified in the protocol
        end

      end

    end
  end
end
